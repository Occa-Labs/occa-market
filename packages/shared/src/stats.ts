/* Aggregate market stats for the landing-page stat bar. */

export type MarketStats = {
  agentsOnline: number;
  totalAgents: number;
  totalUses: number;
  volumeUsd: number;
  providers: number;
};
